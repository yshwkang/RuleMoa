# PRD: 강남대 규정집 챗봇 (RuleMoa)

> 초안입니다. 검토 후 수정 요청 반영합니다.

## 1. 개요

강남대학교 규정집(학칙, 시행세칙 등)을 기반으로 학생들의 규정 관련 질문에 답변하는 챗봇. 학생이 입학연도와 질문을 입력하면, 관련 규정/지침을 근거로 답변을 생성하고 원문 조항을 함께 제시한다.

기술 스택 및 크롤링/DB/배포 아키텍처는 [PLANNING.md](PLANNING.md) 참고.

## 2. 대상 사용자

강남대학교 재학생. 별도 로그인 없이 접근 가능한 것을 전제로 한다 (규정집 원본 데이터 자체가 인증 없이 공개된 정보이므로).

## 3. 핵심 사용자 플로우

**확인 과정**은 "이해한 내용을 그대로 재진술"하는 것이 아니라, **명확한 답변이 어려운 이유를 간단히 설명하고 그에 필요한 질문을 하는 단계**로 정의한다. 학부/학과/전공별로 답변이 달라지는 경우(§3-3)는 이 확인 과정이 발생하는 대표적인 사례다.

### 3-1. 명확성 판단

1. 학생이 **입학연도**(콤보박스, 1990~현재)와 **질문 내용**(텍스트)을 입력
2. 시스템이 검색된 규정을 바탕으로 즉시 최종 답변이 가능한지 판단
   - 가능 → 3-2 진행
   - 불가능(학부/학과/전공별로 다름, 질문 자체가 모호함 등) → 3-3 진행

### 3-2. 명확한 경우: 최종 답변

시스템이 아래 내용을 한 번에 응답
1. 답변 요약
2. 관련 규정/지침 목록 (관련성 높은 순, 최대 5개) — 각 항목마다 규정명, 조항 번호, 관련 부분 발췌, 원문 링크
3. 관련 규정/지침이 하나도 없으면 §4-2의 정책에 따라 안내

### 3-3. 불명확한 경우: 확인 과정 (명확화 질문)

1. 시스템이 답변이 불명확한 이유를 간단히 설명 (예: "이 규정은 학부/학과에 따라 내용이 다릅니다")
2. 명확한 답변을 위해 필요한 질문 제시 (예: "소속 학부/학과를 알려주세요") — 이 단계에서는 최종 답변을 내지 않음
3. 학생이 요청받은 정보를 텍스트로 입력 (예: 소속 학부/학과. 정확한 명칭이 아니어도 됨 — 표기가 다르면 유사도 기반으로 직제규정상 정식 명칭에 매칭하거나, 필요시 재입력/후보 확인을 거침. 상세 규칙은 §4-3 참고)
4. 시스템이 추가 정보를 반영해 3-1로 돌아가 재판단 → 3-2로 진행

## 4. 기능 요구사항

### 4-1. 입력

| 항목 | 형식 | 비고 |
|---|---|---|
| 입학연도 | 콤보박스 | 1990 ~ 현재연도. 서버에서 현재 연도 기준으로 동적 생성 |
| 질문 내용 | 자유 텍스트 | 길이 제한 등은 추후 결정 |
| (예외 플로우) 소속 학부/학과/전공 | 자유 텍스트 | 정식 명칭이 아니어도 유사도 매칭으로 처리 |

### 4-2. 출력

1. **확인 과정** (§3-3 발생 시에만 출력, 질문이 명확하면 이 단계 자체를 생략): 답변이 불명확한 이유를 간단히 설명 + 명확한 답변을 위해 필요한 질문
2. **답변 요약**: 질문에 대한 핵심 답변을 간결하게 요약, **응답 정확도(%)를 함께 표시** (산정 방식: 검색 유사도 기반 자동 산출, §5 참고)
3. **관련 규정/지침 목록**: 관련성 높은 순으로 **최대 5개**, 각각에 대해
   - 규정/지침 명칭
   - 구체적 조항 번호 (예: 제O조 제O항)
   - 관련된 부분만 발췌한 원문 내용 (전체 조항이 아닌 질문과 관련된 부분)
   - 원문을 직접 확인할 수 있는 **링크** — 강남대 규정집 원본 사이트가 아니라, **자체 크롤링 데이터를 기반으로 우리 서비스 내에 만든 조항별 뷰 페이지**로 링크
     - URL 구조: `/rules/{규정명}/{조번호}` (예: `/rules/강남대학교학칙/제5조`) — 원본 사이트의 편/장/호 식별자는 내부 트리 위치일 뿐이라 URL 기준으로 부적합하므로 사용하지 않음. 내부적으로는 안정적인 surrogate key(`regulation_id`, `article_id`)를 별도로 유지해 조인/참조에 사용
     - 렌더링: Next.js ISR, **온디맨드 방식**으로 확정 — 빌드 타임에 전체 조항 페이지를 미리 생성하지 않고 최초 요청 시 렌더링 후 캐시. 크롤러가 재수집을 마친 뒤 on-demand revalidation API를 호출해 갱신
     - 페이지 구성: 규정명, 소속 편/장/절, 조번호+제목, 조문 전체 본문, 최종개정일, 이전/다음 조 네비게이션, 규정 전체 목차 링크
     - 개정으로 조번호가 바뀌거나 삭제되어 링크가 깨진 경우: 하드 404 대신 "이 조항은 개정되어 존재하지 않거나 번호가 변경되었을 수 있습니다" 안내 + 규정 전체 목차로 유도
4. **관련 규정/지침이 0개인 경우**: "학칙, 규정 및 지침 상으로는 답변할 수 없습니다"라는 메시지와 함께, 질문을 더 정확하고 구체적으로 다시 입력하도록 유도하는 안내를 출력 (최종 답변 없이 재질문 대기)

### 4-3. 예외 처리: 학부/학과/전공별 분기

- **판단 기준**: 검색된 관련 규정 중 학부/학과/전공별로 내용이 달라지는 조항이 걸리는 경우 (예: 졸업요건, 이수학점 기준 등 학과마다 다른 규정)
- **추가 입력 요청**: 위 경우, 최종 답변 대신 소속 학부/학과/전공 입력을 요청하는 응답을 반환
- **유사 명칭 매칭**: 학생이 입력한 명칭이 정식 명칭과 다르더라도(학부/학과/전공 표기 혼용 등) 가장 유사한 정식 명칭으로 매칭해 처리
  - **정식 명칭 출처**: 직제규정(크롤링 대상에 포함됨)에 포함된 학부/학과 명칭을 그대로 사용 (별도 목록 구축 불필요)
  - **조직 구조**: "전공" 단위만 독립적으로 존재하는 조직은 없음. 학부 또는 학과 형태로 존재하며, 학부는 보통 2~3개 전공으로 구성됨 (자유전공학부만 예외적으로 내부 전공 구분 없음)
  - **"전공" 입력 처리**: 학생이 "OO전공"이라고 입력하면, 전공-학부 매핑을 자동으로 추론하지 않고 **"학부 또는 학과 명칭으로 다시 입력해주세요"라고 재요청**
  - **매칭 확인 절차**: 학생이 입력한(또는 재입력한) 학부/학과 명칭이 직제규정 목록과 충분히 유사하게 일치하지 않으면, 유사도 상위 **최대 3개**의 학부/학과 명칭 후보를 제시하고 어느 것이 맞는지 학생에게 확인받음

## 5. 비기능 요구사항

- **비용**: LLM/임베딩은 Gemini API 무료 티어 사용, 무료 사용량 초과 시 유료 전환 없이 서비스 응답 중단 ([PLANNING.md](PLANNING.md) 참고)
- **정확성**: 근거 규정 없이 답변을 생성하지 않음(hallucination 방지). 관련 규정을 찾지 못한 경우 §4-2에서 정한 안내 메시지로 명확히 고지
- **출처 검증 가능성**: 모든 답변은 조항 번호와 원문 링크를 통해 학생이 직접 검증 가능해야 함
- **데이터 최신성**: 크롤러가 주기적으로 재수집하여 규정 개정 사항 반영 (크롤링 주기는 PLANNING.md 참고)
- **성공 지표(KPI)**: 응답 정확도를 0~100% 단위로 측정. 검색 유사도(관련 규정 검색 시 벡터 유사도 점수) 기반으로 자동 산출. **매 답변마다 학생에게 노출** (§4-2 참고)

## 6. 범위 제외 (Out of Scope)

- 별표/별지 첨부파일(HWP) 다운로드 및 텍스트 추출 — 별도 논의로 보류
- 로그인/인증, 개인화된 학적 정보 연동 (수강내역, 졸업사정 등 실제 개인 데이터 조회는 미포함)
- 다국어 지원 (한국어 전용)

## 7. 미결정 사항 (Open Questions)

현재까지 제기된 미결정 사항은 모두 결정되었음 (§3, §4, §5 반영). 추가로 논의할 사항이 생기면 이곳에 기록한다.
