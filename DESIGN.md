# DESIGN.md: UI 디자인 규칙 (초안)

> 검토 후 수정 요청 반영합니다. 컬러는 [image/프로모션바2020.png](image/프로모션바2020.png)에서 추출.

## 1. 디자인 방향

- 강남대 재학생에게 익숙한 **카카오톡 스타일 채팅형 UI** 지향 — 딱딱한 규정 정보를 친근한 대화형 인터페이스로 전달
- 강남대 브랜드 컬러(홍보 자료 기준)를 적용해 "학교 공식 서비스"로서의 신뢰감 부여
- 채팅 화면 하나로 입력(입학연도+질문)부터 출력(답변, 관련 규정, 링크)까지 자연스럽게 이어지는 흐름

## 2. 컬러 팔레트

원본 추출값: `image/프로모션바2020.png`에서 파란색 `#0079C2`, 초록색 `#00B185`, 배경 흰색 `#FFFFFF` 확인.

| 토큰 | 값 | 용도 |
|---|---|---|
| Primary Blue | `#0079C2` | 헤더, 챗봇 아이콘, 사용자 말풍선 배경, 주요 버튼 |
| Primary Blue Dark | `#045C94` | 헤딩 텍스트 강조, 클릭 상태 |
| Primary Blue Tint | `#E6F2FA` | 보조 배경, 확인 과정 안내 영역 |
| Accent Green | `#00B185` | 배지/보더 등 면적이 있는 요소, 아이콘. **작은 텍스트나 흰 배경 위 흰 글자 조합에는 사용하지 않음** (아래 Green Text 참고) |
| Green Text | `#067A5A` | 링크, 정확도 배지 텍스트, 우선순위 등 작은 텍스트 — Accent Green은 흰 배경 대비 WCAG 기준(4.5:1) 미달이라 텍스트용으로는 이 진한 톤을 사용 |
| Accent Green Tint | `#E3F8F0` | 규정 카드 배지 배경 |
| Neutral BG (채팅창) | `#EAF1F6` | 채팅 전체 배경 (블루 틴트 그레이) |
| Neutral Border | `#D7E2E8` | 챗봇 말풍선/카드 보더 |
| Text Primary | `#1B2B34` | 본문 텍스트 |
| Text Secondary | `#5E7079` | 타임스탬프, 캡션, 보조 설명 |

## 3. 타이포그래피

| 역할 | 폰트 | 비고 |
|---|---|---|
| 헤딩/강조 | Gothic A1 (Google Fonts) | 둥근 인상, 친근한 무게감 |
| 본문/말풍선 | Noto Sans KR (Google Fonts) | 작은 크기에서도 가독성 우수 |

- 기본 크기: 말풍선/본문 15px, 캡션·타임스탬프 12px, 헤딩 18~20px
- 숫자(정확도%, 조항 번호 등)는 `font-variant-numeric: tabular-nums` 적용

## 4. 레이아웃/컴포넌트 규칙

**전체 구조**: 상단 고정 헤더(챗봇 이름·아이콘) + 스크롤 가능한 채팅 영역 + 하단 고정 입력창

**말풍선**
- 챗봇(좌측): 흰 배경 + `Neutral Border`, 모서리 radius 16px (좌상단만 소폭 각지게 — 말풍선 꼬리 느낌)
- 학생(우측): `Primary Blue` 배경 + 흰 텍스트, radius 16px
- 말풍선 간 세로 gap 8px, 좌우 최대 너비 78%

**입력 영역**
- 하단 고정. 텍스트 입력 + 원형 전송 버튼(`Primary Blue` 배경, 흰색 아이콘 — Accent Green 배경 대비 흰 아이콘의 명도 대비가 부족해 Primary Blue 사용)
- 입학연도 콤보박스는 대화 시작 전 1회 노출(온보딩 단계) 또는 채팅 상단에 고정 배지로 표시

**규정 카드** (챗봇 말풍선 내부)
- 규정명 · 조항 번호 · 발췌 · 원문 링크로 구성된 개별 카드
- 우선순위 배지(1~5, `Accent Green Tint` 배경)를 카드 좌상단에 표시
- 카드 최대 5개, 세로 스택

**정확도 배지**
- 답변 요약 옆에 `%` 표시. 배경은 `Accent Green Tint`, 텍스트는 `Green Text`. 텍스트도 함께 표기(예: "정확도 87%")해 색만으로 정보를 전달하지 않음

**확인 과정(명확화 질문)**
- 챗봇 말풍선 형태 유지
- 학부/학과 후보 제시 시, 자유 텍스트 재입력 대신 **선택형 버튼(최대 3개 후보 + "직접 입력")** 제공

## 5. 인터랙션

- 응답 대기 중: 말풍선 형태의 "..." 타이핑 인디케이터
- 말풍선 등장 시 짧은 fade + slide-up만 적용 (과한 애니메이션 지양)
- `prefers-reduced-motion` 존중 — 설정 시 애니메이션 제거

## 6. 접근성

- 명도 대비 WCAG AA 이상 (Primary Blue 배경 + 흰 텍스트 조합 확인됨)
- 키보드 포커스 상태 명시적 표시
- 우선순위·정확도 등 정보는 색상 단독이 아니라 숫자/텍스트와 병행 전달
